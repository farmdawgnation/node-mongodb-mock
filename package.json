{
  "name": "mongodb-mock",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "./node_modules/.bin/mocha --compilers coffee:coffee-script/register -u tdd -R spec --timeout 2000"
  },
  "dependencies": {
    "coffee-script": "*",
    "underscore": "*"
  },
  "devDependencies": {
    "coffee-script": "1.7.1",
    "chai": "*",
    "mocha": "*"
  },
  "engines": {
      "node": "0.10.x"
  }
}
